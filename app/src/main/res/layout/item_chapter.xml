<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/chapter_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:stateListAnimator="@animator/card_lift"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/colorSurfaceContainer">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="?attr/colorPrimaryContainer">

        <!-- КНОПКА МЕНЮ — ПРАВЫЙ ВЕРХНИЙ УГОЛ, 48dp -->
        <ImageView
            android:id="@+id/menu_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_more_vert"
            android:scaleType="centerInside"
            android:layout_marginTop="4dp"
            app:tint="?attr/colorOnSurfaceVariant"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:contentDescription="Меню главы" />

        <!-- ЗАКЛАДКА — СЛЕВА ОТ МЕНЮ, 32dp -->
        <ImageView
            android:id="@+id/bookmark_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_bookmark"
            android:scaleType="centerInside"
            app:tint="?attr/colorOutline"
            app:layout_constraintTop_toTopOf="@id/menu_button"
            app:layout_constraintBottom_toBottomOf="@id/menu_button"
            app:layout_constraintEnd_toStartOf="@id/menu_button"
            android:layout_marginEnd="8dp"
            android:contentDescription="Закладка" />

        <!-- НАЗВАНИЕ ГЛАВЫ -->
        <TextView
            android:id="@+id/chapter_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Глава 1"
            android:textColor="?attr/colorOnSurface"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/bookmark_icon"
            android:layout_marginEnd="8dp" />

        <!-- ДАТА -->
        <TextView
            android:id="@+id/chapter_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="03.11.25"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@id/chapter_title"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="4dp" />

        <!-- КОЛИЧЕСТВО СЛОВ -->
        <TextView
            android:id="@+id/word_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="225 слов"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@id/chapter_date"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="2dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>